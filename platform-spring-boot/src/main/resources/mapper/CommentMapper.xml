<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.platformspringboot.mapper.CommentMapper">

    <resultMap id="cm" type="Comment">
        <id column="id" property="id"/>
        <result column="video_id" property="videoId"/>
        <result column="user_id" property="userId"/>
        <result column="parent_id" property="parentId"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (video_id, user_id, parent_id, content) VALUES (#{videoId}, #{userId}, #{parentId}, #{content})
    </insert>

    <select id="listByVideo" resultMap="cm">
        SELECT * FROM comment WHERE video_id=#{videoId} AND status=1 ORDER BY id DESC LIMIT #{offset}, #{size}
    </select>

    <select id="countByVideo" resultType="long">SELECT COUNT(*) FROM comment WHERE video_id=#{videoId} AND status=1</select>

    <delete id="deleteById">DELETE FROM comment WHERE id=#{id} AND user_id=#{userId}</delete>
</mapper>
